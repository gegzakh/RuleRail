{
  "name": "rulerail-demo",
  "version": "1.0.0",
  "private": true,
  "description": "RuleRail payment standards intelligence and client readiness demo",
  "scripts": {
    "build": "node scripts/verify-static.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check dist/analytics.js && node --check dist/validation.js && node --check dist/app.js && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
